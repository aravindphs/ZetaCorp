# No environment variables required.
# Forms use FormSubmit (https://formsubmit.co) — zero setup, no account needed.
# The first submission to contact@zetacorpsolutions.com will trigger a
# one-time confirmation email from FormSubmit — just click Activate.
